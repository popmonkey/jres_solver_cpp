name: Windows CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  windows:
    uses: ./.github/workflows/win-build.yml
    secrets: inherit
    with:
      upload_assets: false
      upload_url: ""
